apiVersion: v1
kind: Service
metadata:
  name: dev-apigateway-service
  namespace: dev
spec:
  type: ClusterIP
  selector:
    app: dev-apigateway
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
